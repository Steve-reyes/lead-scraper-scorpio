# Backend configuration for Lead Scraper

# HTTP API port
PORT=4000

# WebSocket port (separate from HTTP, used for real-time streaming)
WS_PORT=4001

# Chrome DevTools Protocol endpoint
# Point this at a running Chromium instance in headless mode
CHROME_CDP_URL=ws://127.0.0.1:3012
